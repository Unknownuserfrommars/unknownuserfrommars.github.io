# kevin-z.com

Source for Kevin Zhou's personal site.

- Edit `_body.html` for page content and styling.
- Edit `build.js` for document metadata and the standalone wrapper.
- Run `node build.js` to regenerate `index.html` before publishing.

The generated `index.html` is intentionally dependency-free at runtime except
for the optional Google Fonts stylesheet used by the design.
